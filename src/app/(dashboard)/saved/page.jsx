"use client";import {useEffect,useState} from "react";import {apiClient} from "@/lib/api-client";import OpportunityCard from "@/components/opportunity-card";import EmptyState from "@/components/empty-state";import PageHeader from "@/components/page-header";export default function Page(){const[data,setData]=useState([]),[status,setStatus]=useState("Loading...");useEffect(()=>{apiClient("/api/v1/saved-opportunities").then(r=>{setData(r.data||[]);setStatus("")}).catch(e=>setStatus(e.message))},[]);return <div><PageHeader eyebrow="Bookmarks" title="Saved Opportunities"/>{status&&<p>{status}</p>}{!status&&!data.length?<EmptyState title="No saved opportunities" description="Save opportunities while browsing." href="/opportunities" label="Explore opportunities"/>:<div className="grid gap-6 sm:grid-cols-2 xl:grid-cols-3">{data.map(o=><OpportunityCard key={o._id} opportunity={o}/>)}</div>}</div>}
