"use client";import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {registerWithEmail,loginWithGoogle} from "@/lib/auth-service";export default function Page(){const router=useRouter(),[form,setForm]=useState({name:"",email:"",password:""}),[error,setError]=useState("");const submit=async e=>{e.preventDefault();const r=await registerWithEmail(form);if(r.error)setError(r.error.message);else{router.push("/dashboard");router.refresh()}};return <div className="card w-full max-w-md p-8"><h1 className="text-3xl font-bold">Create account</h1><button onClick={()=>loginWithGoogle()} className="mt-6 w-full rounded-xl border p-3 font-bold">Continue with Google</button><form onSubmit={submit} className="mt-6 grid gap-4"><input required placeholder="Full name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="rounded-xl border p-3"/><input required type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="rounded-xl border p-3"/><input required minLength={8} type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="rounded-xl border p-3"/><button className="rounded-xl bg-indigo-600 p-3 font-bold text-white">Create account</button></form>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<p className="mt-6 text-sm">Already registered? <Link href="/login" className="font-bold text-indigo-600">Log in</Link></p></div>}
