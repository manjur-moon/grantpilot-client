"use client";import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";import {loginWithEmail,loginWithGoogle,loginWithDemoAccount} from "@/lib/auth-service";export default function Page(){const router=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");const done=()=>{const redirect=new URLSearchParams(window.location.search).get("redirect")||"/dashboard";router.push(redirect);router.refresh()};const submit=async e=>{e.preventDefault();const r=await loginWithEmail({email,password});if(r.error)setError(r.error.message);else done()};return <div className="card w-full max-w-md p-8"><h1 className="text-3xl font-bold">Welcome back</h1><button onClick={async()=>{const r=await loginWithGoogle();if(r?.error)setError(r.error.message)}} className="mt-6 w-full rounded-xl border p-3 font-bold">Continue with Google</button><form onSubmit={submit} className="mt-6 grid gap-4"><input type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="rounded-xl border p-3"/><input type="password" required placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="rounded-xl border p-3"/><button className="rounded-xl bg-indigo-600 p-3 font-bold text-white">Log in</button></form><button onClick={async()=>{const r=await loginWithDemoAccount();if(r.error)setError(r.error.message);else done()}} className="mt-3 w-full rounded-xl bg-amber-100 p-3 font-bold text-amber-900">Use demo account</button>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<p className="mt-6 text-sm">No account? <Link href="/register" className="font-bold text-indigo-600">Register</Link></p><Link href="/forgot-password" className="mt-2 block text-sm text-indigo-600">Forgot password?</Link></div>}
