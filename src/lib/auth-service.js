import { authClient } from "./auth-client"; export const loginWithEmail=({email,password,rememberMe=true})=>authClient.signIn.email({email,password,rememberMe}); export const registerWithEmail=({name,email,password})=>authClient.signUp.email({name,email,password}); export const loginWithGoogle=()=>authClient.signIn.social({provider:"google",callbackURL:"/dashboard"}); export const logoutUser=()=>authClient.signOut(); export const loginWithDemoAccount=()=>loginWithEmail({email:process.env.NEXT_PUBLIC_DEMO_EMAIL,password:process.env.NEXT_PUBLIC_DEMO_PASSWORD}); export const sendPasswordResetRequest=(email)=>authClient.requestPasswordReset({email,redirectTo:"/reset-password"}); export const updatePasswordWithToken=({token,newPassword})=>authClient.resetPassword({token,newPassword});
