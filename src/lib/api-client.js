export async function apiClient(path,{method="GET",body,query,signal,headers={}}={}){let url=path;if(query){const p=new URLSearchParams();Object.entries(query).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=="")p.set(k,String(v));});const s=p.toString();if(s)url+=`?${s}`;}const isForm=body instanceof FormData;const response=await fetch(url,{method,credentials:"include",signal,headers:{...(isForm?{}:{"Content-Type":"application/json"}),...headers},body:body===undefined?undefined:isForm?body:JSON.stringify(body)});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||`Request failed (${response.status})`);return data;}
