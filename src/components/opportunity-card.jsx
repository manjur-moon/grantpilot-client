import Link from "next/link";import {formatCurrency,formatDate,formatStatus} from "@/lib/formatters";export default function OpportunityCard({opportunity}){return <article className="card overflow-hidden"><img src={opportunity.coverImage} alt={opportunity.title} className="h-44 w-full object-cover"/><div className="p-5"><p className="text-xs font-bold uppercase text-indigo-600">{formatStatus(opportunity.category)}</p><h2 className="mt-2 line-clamp-2 text-lg font-bold">{opportunity.title}</h2><p className="mt-1 text-sm text-slate-500">{opportunity.providerName}</p><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{opportunity.shortDescription}</p><div className="mt-4 grid gap-1 text-sm"><span>Funding: {formatCurrency(opportunity.funding?.amount,opportunity.funding?.currency)}</span><span>Deadline: {formatDate(opportunity.deadline)}</span></div><Link href={`/opportunities/${opportunity.slug}`} className="mt-5 block rounded-xl bg-indigo-600 px-4 py-3 text-center text-sm font-bold text-white">View details</Link></div></article>}
